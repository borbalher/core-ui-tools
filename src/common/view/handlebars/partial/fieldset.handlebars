<fieldset id="{{id}}"
  {{#if name}}name="{{name}}"{{/if}}
  {{#if formId}}form="{{formId}}"{{/if}}
  {{#if disabled}}disabled{{/if}}
  {{#each datasets as |dataset|}}
    {{#with dataset}}data-{{name}}="{{value}}"{{/with}}
  {{/each}}>

  {{#if legend}}
    <legend>
      <span>{{legend}}</span>
    </legend>
  {{/if}}

  {{> @partial-block }}

</fieldset>