module.exports = {
  core :
  {
    locator :
    {
      'aggregate/page'       : `${__dirname}/aggregate/page`,
      'aggregate/state'      : `${__dirname}/aggregate/state`,
      'aggregate/view-model' : `${__dirname}/aggregate/view-model`
    }
  }
}
